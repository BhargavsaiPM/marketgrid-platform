# =============================================================
# Vendor Service — centralised configuration
# =============================================================
# This file will hold configuration for the vendor-service, e.g.:
#   - datasource (URL, username, password)
#   - vendor onboarding settings
#   - service-specific feature flags
#
# Values will be populated phase by phase.
# =============================================================

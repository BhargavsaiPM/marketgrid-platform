server:
  port: 8084

spring:
  application:
    name: order-service
  config:
    import: configserver:http://localhost:8888
  datasource:
    url: jdbc:h2:mem:orderdb;DB_CLOSE_DELAY=-1

service:
  account:
    username: admin
    password: Admin@12345
